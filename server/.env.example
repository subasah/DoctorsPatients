PORT=8787

# OpenAI (optional; without it endpoints will return a helpful error)
OPENAI_API_KEY=
OPENAI_TRANSCRIBE_MODEL=whisper-1
OPENAI_CHAT_MODEL=gpt-4o-mini

# CORS (set to your Expo dev origin in production)
CORS_ORIGIN=*

# Optional: FHIR target (Epic or other)
FHIR_BASE_URL=
FHIR_ACCESS_TOKEN=
